<%= render('errors', :object => @image) %>
<%= form_for @image, html: {mutipart: true } do |f| %>

  <%= f.label :files %>
  <%= f.text_field :files %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :img, "Image" %>
  <%= f.file_field :img %>

  <%= f.submit %>
<% end %>
